# Инструкция по использованию

## Быстрый старт

### Компиляция

```bash
./compile.sh
```

или

```bash
javac -d target/classes -encoding UTF-8 src/main/java/com/task2/*.java
```

### Запуск программы с собственной реализацией списка

```bash
java -cp target/classes com.task2.Main
```

### Запуск программы с ArrayList

```bash
java -cp target/classes com.task2.MainArrayList
```

### Запуск теста измерения шагов

```bash
java -cp target/classes com.task2.StepMeasurementTest
```

## Использование программы

1. После запуска программа ожидает ввода строк
2. Введите строки для добавления в список (они будут добавлены в начало)
3. Строки длиннее 80 символов автоматически разрезаются на несколько строк
4. Введите пустую строку для вывода текущего состояния списка
5. Введите `exit` для завершения программы и вывода общего количества шагов сортировки

## Пример использования

```
Программа запущена. Введите строки (пустая строка для вывода списка, 'exit' для выхода):
zebra
apple
banana

--- Текущее состояние списка ---
0: banana
1: apple
2: zebra
--- Конец списка ---
Всего шагов сортировки на данный момент: 15

cherry

--- Текущее состояние списка ---
0: apple
1: banana
2: cherry
3: zebra
--- Конец списка ---
Всего шагов сортировки на данный момент: 42

exit
Всего шагов сортировки: 45
```

## Настройка параметров

Параметры можно изменить в файлах `Main.java` и `MainArrayList.java`:

- `NUM_SORTING_THREADS` - количество потоков сортировки (по умолчанию 2)
- `DELAY_MS` - задержка между шагами и внутри шагов в миллисекундах (по умолчанию 1000 мс = 1 секунда)
- `MAX_LINE_LENGTH` - максимальная длина строки перед разрезанием (по умолчанию 80 символов)

## Теоретический расчет шагов

Для расчета теоретического максимального количества шагов:

**Формула:** `N × (T / (2D))`

где:
- N - количество потоков сортировки
- T - время работы программы в миллисекундах
- D - задержка в миллисекундах

**Пример:**
- 2 потока
- 5 секунд работы (5000 мс)
- 100 мс задержка

Максимальное количество шагов: `2 × (5000 / (2 × 100)) = 2 × 25 = 50 шагов`

**Примечание:** Это теоретический максимум. Фактическое количество шагов зависит от:
- Конкуренции потоков
- Блокировок
- Планировщика потоков JVM
- Нагрузки системы

